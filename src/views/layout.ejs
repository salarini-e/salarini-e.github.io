<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', { 
    mobileRedirect: true, 
    redirectPath: '/' 
}) %>
<body>
    <!-- Preloader -->
    <%- include('partials/preloader') %>

    <!-- Modal customizado do menu -->
    <%- include('partials/menu-modal', {page: page}) %>
    
    <!-- Canvas de fundo -->
    <%- include('partials/background-canvas') %>
    
    <!-- Header -->
    <%- include('partials/header') %>
    
    <!-- Conteúdo principal -->
    <main class="container-fluid custom-animated slide-in-up custom-hidden">
        <%- body %>
    </main>

    <!-- Footer -->
    <%- include('partials/footer') %>

    <!-- Scripts -->
    <%- include('partials/scripts', { 
        additionalJS: (typeof scripts !== 'undefined' && scripts.length > 0) ? scripts : null 
    }) %>
    
</body>
</html>
